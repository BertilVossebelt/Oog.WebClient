version: '3.8'

services:
  webclient:
    build: .
    container_name: oog-webclient
    ports:
      - "4000:80"
    restart: unless-stopped
    networks:
      - oog-network

networks:
  oog-network:
    external: true
